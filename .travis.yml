dist: xenial
language: python
python:
  - "3.6"
  - "3.7"
  - "3.7-dev"
  - "3.8-dev"

sudo: false

install:
  - pip install numpy pandas codecov torch
  - pip install -e .

script:
  - pytest --cov=./

after_success:
  - codecov
